spring:
  application:
    name: pustaka


  sql:
    init:
      mode: always

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  datasource:
    url: jdbc:postgresql://localhost:5432/pustaka
    username: postgres
    password: kunammanuk
    driver-class-name: org.postgresql.Driver

